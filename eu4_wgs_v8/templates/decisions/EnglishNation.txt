country_decisions = {

	english_nation = {
		major = yes
		potential = {
			NOT = { exists = ENG }
			NOT = { exists = GBR }
			NOT = { tag = HLR }
			NOT = { tag = PAP }
			primary_culture = english
		}
		allow = {
			is_at_war = no
			OR = {
				num_of_owned_provinces_with = {
					value = 8
					OR = {
						region = northumbria
						region = mercia
						region = essex
						region = welsh_region
					}
				}
				NOT = {
					any_province = {
						OR = {
							region = northumbria
							region = mercia
							region = essex
							region = welsh_region
						}
						NOT = { owned_by = ROOT }
					}
				}
			}
		}
		effect = {
			northumbria = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_claim = ENG
			}
			mercia = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_claim = ENG
			}
			essex = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_claim = ENG
			}
			welsh_region = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_claim = ENG
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			add_prestige = 25
			change_tag = ENG
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
}
